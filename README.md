# BLife

A web-based life simulation game — inspired by Conway's Game of Life, but built around its own rules and parameters.

Built with plain HTML, CSS, and JavaScript (no frameworks, no build step).

## Status

Early planning stage. See `docs/` for design documentation.
